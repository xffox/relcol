#!/usr/bin/env python

from distutils.core import setup

setup(name = 'relcol',
        version = '0.1',
        description = "Music releases info collector",
        author = "xffox",
        author_email = "0xff0x666@gmail.com",
        packages = ['relcol'],
        scripts = ['scripts/relcol']
        )
